import { useAuth } from '../context/AuthContext'
export default function Profile() { const { user } = useAuth(); const items = [['Full name', user.full_name], ['Email address', user.email], ['Account role', user.role], ['Age', user.age ?? 'Not provided'], ['Gender', user.gender || 'Not provided'], ['Address', user.address || 'Not provided'], ...(user.category ? [['Specialization', user.category]] : [])]; return <><section className="page-heading"><p className="eyebrow">ACCOUNT</p><h1>Your profile</h1><p>Your account details as held by MediVeria.</p></section><section className="profile-card"><div className="profile-identity"><span className="profile-avatar">{user.full_name.split(' ').map(x => x[0]).slice(0,2).join('')}</span><div><h2>{user.full_name}</h2><p>{user.role} account</p></div></div><dl>{items.map(([label, value]) => <div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</dl></section></> }
